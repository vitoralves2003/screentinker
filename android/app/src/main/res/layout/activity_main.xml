<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/rootLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000"
    android:keepScreenOn="true">

    <!-- ExoPlayer Video View -->
    <androidx.media3.ui.PlayerView
        android:id="@+id/playerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:use_controller="false"
        app:resize_mode="fit"
        app:surface_type="texture_view"
        android:visibility="gone" />

    <!-- Image View for static images -->
    <ImageView
        android:id="@+id/imageView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitCenter"
        android:visibility="gone"
        android:contentDescription="Display content" />

    <!-- WebView for YouTube embeds -->
    <WebView
        android:id="@+id/youtubeWebView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone" />

    <!-- Status Overlay -->
    <LinearLayout
        android:id="@+id/statusOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical"
        android:background="#000000"
        android:visibility="visible">

        <!-- The wordmark, rather than a generic glyph at 30% opacity beside the name typed out
             again. This is what a customer looks at while the panel connects, and on a wall it is
             often the only thing showing for a minute at a time. adjustViewBounds keeps the
             428x102 asset's proportions instead of stretching it into the box. -->
        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="72dp"
            android:adjustViewBounds="true"
            android:src="@drawable/loop_player_logo"
            android:layout_marginBottom="16dp"
            android:contentDescription="@string/player_title" />

        <TextView
            android:id="@+id/statusText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/player_connecting"
            android:textColor="@color/text_secondary"
            android:textSize="16sp" />

        <!--
             DUAS ESPERAS DIFERENTES, DOIS DESENHOS.

             A roda gira quando não há o que medir — conectando, esperando conteúdo, nada agendado.
             Ela é honesta ali: ninguém sabe quanto falta.

             A barra aparece só quando existe uma resposta para "quanto falta": os arquivos da lista
             estão sendo baixados, e o player sabe quantos bytes são e quantos já estão em disco. Uma
             barra que anda é a diferença entre "isto está funcionando" e "isto travou" — que é a
             pergunta de quem está instalando um painel e olhando para ele.

             Mostrar as duas ao mesmo tempo seria dizer a mesma coisa duas vezes, então quem mostra a
             barra esconde a roda.
        -->
        <ProgressBar
            android:id="@+id/statusSpinner"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginTop="24dp"
            android:indeterminate="true" />

        <ProgressBar
            android:id="@+id/statusBarra"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="280dp"
            android:layout_height="6dp"
            android:layout_marginTop="24dp"
            android:max="100"
            android:progress="0"
            android:progressDrawable="@drawable/barra_de_carga"
            android:visibility="gone" />
    </LinearLayout>

    <!-- #109: PiP overlay layer (otherwise-transparent, empty). PipOverlay renders the
         overlay box into this; the playlist renderers never touch it.
         NOTE: at runtime MainActivity REPARENTS this out of rootLayout up to the window
         content (android.R.id.content) so it composites ABOVE the YouTube WebView's video
         plane (the in-rootLayout position was occluded — see #109). MainActivity mirrors
         rootView's orientation/wall transform onto it so corner positions still track the
         rotated content. It is declared here only so it inflates with a known id. -->
    <FrameLayout
        android:id="@+id/pipLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

</FrameLayout>
